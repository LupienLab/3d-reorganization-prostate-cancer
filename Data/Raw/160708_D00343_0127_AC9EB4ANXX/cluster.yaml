__default__:
    params: '-V -cwd -q lupiengroup'

fastqc:
    params: '-V -cwd -q light.q'

align_hg38:
    params: '-V -cwd -q highmem.q -l h_vmem=16G -l mem_requested=16G'

align_hg19:
    params: '-V -cwd -q highmem.q -l h_vmem=16G -l mem_requested=16G'

call_peaks:
    params: '-V -cwd -q lupiengroup -l h_vmem=12G -l mem_requested=12G'