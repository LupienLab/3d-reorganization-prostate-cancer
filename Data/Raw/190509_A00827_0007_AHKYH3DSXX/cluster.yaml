__default__:
    params: "-V -d . -q himem -l vmem=60gb -l nodes=1:ppn=8"

fastqc:
    params: "-V -d . -q all"

sort_bam:
    params: '--export=ALL -p himem --mem=30G -c 8 -t 4-00:00:00'

sort_bam_name:
    params: '--export=ALL -p himem --mem=30G -c 8 -t 4-00:00:00'

create_pairs:
    params: "--export=ALL -p himem --mem=30G -c 8 -t 4-00:00:00"

create_cooler:
    params: "--export=ALL -p himem --mem=30G -c 8"

adjust_res:
    params: "--export=ALL -p himem --mem=30G -c 8"
